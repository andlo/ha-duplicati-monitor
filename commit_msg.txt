docs: add New data, Modified data, Duration columns to dashboard

- "Recent runs" table: added New data / Modified data (filesizeformat)
  and Duration columns between Files and W/E.
- "Backup jobs - status" card: converted from an auto-entities
  entities card to a markdown table, since attributes nested inside
  the history sensor's runs list aren't accessible from a plain
  entities row. Shows each job's current status plus the last run's
  New data, Modified data, and Duration, matched via the *_status ->
  *_history entity_id suffix swap.
