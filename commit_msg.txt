Rebalance dashboard top row 50/50 after live layout review

andlo's live dashboard showed the top row split 25/75 (gauge+jobs vs
the status list) rather than evenly, and the "Recent runs" table
still wasn't using its full width despite the section/grid_options
already being correct per Home Assistant's documented sizing rules -
confirmed by pulling the live config back out unchanged.

Rebalanced the top row to a 50/50 column_span split (concrete,
low-risk proportion change). Left the table-width issue itself
unresolved for now and asked andlo to enable the dashboard-screenshot
beta feature so it can be verified visually instead of guessed at
further - config-level checks have confirmed the JSON is correct, so
whatever's happening now is a rendering/CSS question that needs an
actual screenshot to diagnose properly.

No custom_components/ changes - dashboard-only, no new release.
